let fruits = ["Apple", "Banana", "Cherry"];
console.log(fruits[0]); // Accessing elements
fruits.push("Mango");   // Adding an element
console.log(fruits);


/* methods
// 1️⃣ push() - Adds an element to the END of an array
let fruits = ["Apple", "Banana"];
fruits.push("Cherry");
console.log(fruits); // Output: ["Apple", "Banana", "Cherry"]

// 2️⃣ pop() - Removes the LAST element from an array
let lastFruit = fruits.pop();
console.log(fruits); // Output: ["Apple", "Banana"]
console.log(lastFruit); // Output: "Cherry"

// 3️⃣ map() - Transforms each element in an array
let numbers = [1, 2, 3, 4, 5];
let squaredNumbers = numbers.map(num => num * num);
console.log(squaredNumbers); // Output: [1, 4, 9, 16, 25]

// 4️⃣ filter() - Filters elements based on a condition
let evenNumbers = numbers.filter(num => num % 2 === 0);
console.log(evenNumbers); // Output: [2, 4]
*/